# 开发计划（2026-09-21，审查后重订）

上一版计划（`D:\E(A, B)\07 程序设计·统一接触内核方案.md`）在 09-19 被实测推翻五条、补两条；
这一版再按 09-21 的独立审查（`reports/audit_20260921.md`：31 条确认缺陷、6 处盲区）重订。

**目标不变**：把 DDA 的接触层重写成以 E(A,B) 为内核、域无关的接触引擎；DDA 是第一个域适配器。

**预算单位不变**：门与反例的数量，不是周。三天跑完了原计划十五周的内容，然后审查抓出 2 critical + 15 major——
瓶颈从来不是写代码，是知道代码对不对。

## 一、审查逼出来的三条新纪律（写进 AGENTS.md，每个里程碑都受它约束；2026-09-24 补 D、E 两条，见本节末，已写进 AGENTS.md §2a）

**纪律 A · oracle 不得与被测代码共用几何原语。** 这次 4 条 major 都是"oracle 和代码一起错"：扇形三角化两边都用、
半平面核两边都用、sign(min gap) 在二维当 oracle。反面例子恰好也是这次最强的验证——L3 极限分析与 L5 逃逸剖面
零共用代码，所以两者相等才有意义。以后每道 G0 类门必须在 docstring 里写明"oracle 用了哪些原语，与被测对象不重合"。

**纪律 B · 参数化必须按构造覆盖三类格子。** 每个参数化门至少含：一个分界点（如 μ = w/2h）、一个非对称/一般位置输入
（如 nx=5 而不只 nx=4）、一个尺度变化输入（荷载 ×100、几何 ×1e-3）。这次 critical 1 与 major 7、9 全是"参数恰好避开"。

**纪律 C · 声称先门后文。** README/reports 里任何带 ✔ 的句子必须能指向一个以该声称命名的测试；docstring 里的性质
陈述（"不施加 in_extent 过滤"、"三票"、"贯入时恒为 0"）同样。这次四条假声称全是先写文档后（没）写门。

**（2026-09-24 补）纪律 D · 里程碑的绿以新鲜 git worktree 为准，主树的绿不算数。** M0 开新 worktree 时才发现：
10 个 Phase 0 夹具当初被系统级 `core.autocrlf=true` 规范成 LF 入库，而 `fixtures/PROVENANCE.json` 记的是 CRLF 原件的哈希——
主树工作副本仍是 CRLF、stat 缓存让 `git status` 显示干净，所以溯源门只在主树上绿，任何新克隆都红。09-21 六路审查全在主树里跑，
一路都没抓到。cc5d7cf 修复（`* -text` 下 `git add --renormalize`，本地 `core.autocrlf=false`）。
以后每个里程碑收尾、每次合并后，都在新建的 worktree 或全新克隆里跑全套，必须全绿。

**（2026-09-24 补）纪律 E · 镜像/中心对称位形会藏住符号错误，参数化必须含非对称参考点。** 写端到端门时发现：
cb2 的四个接触点关于原点中心对称，"接触列力矩反号"（等价于把每个力挪到 −p）与"接触列忘减参考点"两种缺陷在原点取对称中心时
完全不可见。`tests/test_end_to_end_cb2.py::test_capacity_is_independent_of_the_moment_reference_point` 把参考点挪到
(5,−3,2)、(−0.7,0.4,−11)，两种变异下各有 4 例报红（2 个非零原点 × μ ∈ {0.3, 1.5}；2026-09-24 本次复核）。
这是纪律 B"非对称输入"的一个具体推论：不只几何要非对称，**参考点**也要。

再加一道**G6 独立对抗审查**：每个里程碑收尾必须跑一次与 09-21 同构的六路审查，
**0 critical、0 major 才算过**；声称在 G6 之后才许进 README。这是唯一诚实的退出判据。

## 二、里程碑

### M0 · 止血：修 31 条、堵 6 个盲区，然后重审

按"什么在作废一条声称"排序，不按文件。

**M0.a 数值根基（两条 critical + 一条 major）**
- `lp.py`：一阶段可行性改相对阈值 `tol × max(1, max b)`（或行归一化）；`limit.py` 停止把 tol 乘 1e-3。
  回归门：`_interlock_capacity(0.2,0.1,k=8)`、`(0.3,0.1,k=16)` 必须 optimal 且等于 Patton；`lp.solve` 对 b 缩放 1e2…1e6 的同一系统全 optimal。
- `covers3.membership_convex3`：改用 E 的全部支撑半空间（B 每个面法向取 A 上支撑顶点，反之亦然）+ 严格 EE。
  回归门：tetra vs box 在 t ∈ {1.0, 1.5, 2, 3} 全判分离；两个轴对齐方块不崩溃；`g0_convex3(tetra, box)` 零失配。
  - **订正（2026-09-24）**：t = 1.0 不是分离，是恰好面接触（闭式 φ = 0），正确判决是 **0（相触）**；只有 t > 1 才判 −1。
    门按闭式解析 φ 逐点判：`tests/test_covers3_membership.py::test_membership_tetra_vs_box_sweep_along_x`
    （t = 1.0 → 0，t = 1.001 / 1.5 / 2 / 3 → −1，另含 t = −2.0 这一侧的分界）。
    "B 每个面法向取 A 上支撑顶点，反之亦然"写明为：B 的面（外法向 n）取 a* = argmin_A n·a；
    A 的面（外法向 m）在 E 上对应的法向是 **−m**，支撑顶点取 b* = argmin_B m·b（`covers3.support_gaps3`）。
    两个轴对齐方块不崩溃、tetra vs box 零失配分别由同文件的 `test_membership_axis_aligned_boxes_does_not_crash`、
    `test_g0_convex3_tetra_vs_box_has_zero_mismatches` 看护。
- `limit.py`：列构造抽成 `_build_columns`，`limit_load` / `check_duality` / `mechanism_is_admissible` 三处共用；
  对偶解切成 `(y_wrench, y_cohrow)`，上限加回 Σy_row。回归门：带黏聚力的 `check_duality` 对偶间隙 < 1e-9、互补 < 1e-9。

**M0.b 非凸面几何（四条 major）**
- 一个 `geom3.point_in_face_polygon(x, P, fi, tol)`：先量到各棱段距离 ≤ tol 即真（保留边界容差语义），否则投影到主轴平面做环绕数。
  `_point_inside_face`、`_point_face_distance`、`_point_on_face`、`segment_crosses_face`、射线奇偶五处全部换用。
  回归门：L 块十边形侧面上落在"臂"里的顶点 in_extent=True；细杆 vs L 块分离判 −1；`brute_feature_distance` 对 L 块与暴力顶点采样一致。
- `polyhedra_overlap`：网格采样前加"相触顶点沿内法向和微推"证人步。回归门：L 块凹槽咬 0.05 方块判 1（体积 0.012）。
  - **订正（2026-09-24）**：这两个数对不上。咬深 e 时交集体积是 4e + e²，体积 0.012009 对应 **e = 0.003**；
    e = 0.05 旧实现本来就判得出。真正的盲区是 e ≲ 0.01。门现在覆盖 e ∈ {0.01, 0.003, 1e-4, 1e-6}，
    外加只咬一面墙、两面咬深不同两种非对称咬法，以及恰好填满（判 0）、留缝（判 −1）两个分界，
    每例在 5 种摆放（含 ×1e-3、×1e3 与随机旋转）下双向判：`tests/test_geom3_overlap_witness.py::test_thin_notch_bite_is_detected`；
    审查原例的体积由 `test_audit_case_volume_is_0_012` 钉住。
- `merge_coplanar`：平面组内按共棱并查集分连通分量。回归门：pinch 顶点几何不死循环；U 形棱柱两端面各自保留；体积不变、流形 clean。
  - **订正（2026-09-24，补全语义）**：只在一个顶点相触的共面面属于不同的共棱分量，**保持不归并**
    （`tests/test_geom3_merge.py::test_vertex_pinched_coplanar_faces_do_not_hang`，输出逐面等于输入）。
    若 pinch 或孔洞出现在**同一个**共棱连通分量内部，这块区域不是简单多边形、做不成一个面，
    实现**明确抛 ValueError**，既不归并也不挂死（`test_pinch_inside_an_edge_connected_group_raises_clearly`、
    `test_hole_inside_an_edge_connected_group_raises_clearly`）。
- `ve3_cover` / `ev3_cover`：strict 按锥判据如实计算；**同时删除**低维盖的 strict 过滤门（保留会破坏距离完备性，已实测）。

**M0.c 理论-代码一致（五条 major + 若干 minor）**
- `interlock.escape_candidates`：`require_in_extent=False`，与 docstring一致。回归门：`interlocking_pair(5,2,0.3)` 25 个随机偏移 vs 暴力二分零失配。删除 `verify=False` 分支。
  - **订正（2026-09-24）**：(1) 25 个偏移的主 oracle 不是暴力二分，而是**不调用任何几何谓词的解析逃逸高度**
    （`tests/test_interlock_escape.py::analytic_escape_height`）。暴力二分 `escape_height_brute` 与被测路径共用
    `polyhedra_overlap`（违反纪律 A），每个偏移约 1 s（本机 `--durations` 0.9–1.4 s），在审查用的 nx=5（phase 1.0）块上
    只在 4 个偏移上作佐证（`test_analytic_oracle_agrees_with_brute_bisection` 3 个 + `test_audit_offset_matches_brute_and_the_truth_is_a_candidate` 1 个）；
    另有 nx=4 块上文档反例的 3 个 δ（0.2 / 0.4 / 0.6）也用它对照
    （`test_public_escape_entry_matches_brute_on_the_documented_counterexample`）。
    25 偏移零失配的门是 `test_escape_height_covers_is_complete_off_axis_on_asymmetric_blocks`，跑两块：
    本行原文的 (5,2,0.3, phase 0) 与审查用的 phase 1.0。
    (2) 修法不只是 `require_in_extent=False`：候选按**各自 z\* 处**的 in_extent 过滤。不过滤的候选集会混进
    延长平面上碰巧并列的盖，它们的冻结导数没有意义——修复者实测 δ=0.3 处并列胜出的错盖给 ∂h/∂amp = 4.6，
    真值 0.6（出处：`src/eab/kernel3d/interlock.py` 候选过滤注释；4.6 这个数本次**未独立复现，待核**；
    真值 0.6 = 2δ 由 `tests/test_interlock_sensitivity.py::test_dh_damp_is_two_delta_on_the_rising_branch[0.3]` 钉住）。
- `margin.body_distance` / `margin_contacts`：入口 overlap 守卫；贯入返回哨兵接触（distance 0、margin_gap −δ、closed=True、normal None）。
  回归门：贯入输入下 `margin_contacts` 非空且 closed；与 `inflated_membership` 一致。改模块文档与 `reports/margin_layer.md` 的"贯入"口径。
- `frozen.py`：补 VE3 / EV3 两个泛型分支（顶点到棱直线投影残差）。回归门：400 个随机分离位形，`body_distance` 标签全部进冻结路径零崩溃、gap 与见证距离 < 1e-12、∂/∂δ ≡ −1。
- `tia_sensitivity.sensitivity_at`：冻结后在 δ±ε、phase±ε 重定标签，标签变或单侧差商不一致 → `differentiable=False`；`profile()` 输出该列。
  回归门：phase=0 默认剖面每行 `differentiable=False`（偶函数拐点）；phase=0.4 处 True 且与中心差分一致。
  - **订正（2026-09-24）**：实现没有用"δ±ε、phase±ε 重定标签"。可微标志取的是**全部并列活动盖的导数离散度**——
    并列盖在一阶上给出同一导数才算可微，这是精确的一阶判据，不依赖 ε。它对照解析逃逸高度的两个单侧差商检验：
    `tests/test_interlock_sensitivity.py::test_differentiability_flag_agrees_with_one_sided_quotients`（9 组，含非对称块、网格折点、幅值 ×1e-2）；
    phase=0 默认剖面 19 行全部 `differentiable=False` 且导数区间等于解析单侧值：`test_phase_derivative_at_phase_zero_is_flagged_on_every_default_row`；
    phase=0.4 一般位置：`test_generic_point_is_differentiable_and_matches_central_differences`。
- `dual.py`：删 `__float__`（或抛 TypeError）；`atan2` 宽度校验；`__pow__` 负底非整数幂抛错。`osteomorphic_block_generic` 默认 `sin_fn = eab.dual.sin`。
  回归门：`math.sin(Dual)` 抛 TypeError；不传 sin_fn 时 ∂h/∂phase 非零。
- `limit.contacts_from_covers`：接触点改取 `point_a`（动体表面），`side="a"|"b"|"mid"`。回归门：gap=0.3 的倾覆算例 3.3333 与 gap 无关。
- 二维读取器（`bdda_geom.load_step_geometries`、`bb52_g0.load_blocks`）：剥除**所有**连续重复顶点并登记 alias。

**M0.d 门补牙（六条 gate-quality）**
- `g03.g0_convex3`：真正实现票二——冻结盖法向集合 vs 凸包面法向集合（归并共面后），`passed` 看它。回归门：删任一严格 facet 必红（190/190）。
  - **订正（2026-09-24）**：票二**没有**用 `merge_coplanar`。凸包 facet 法向取自三角形叉积，按差点集上的支撑定向、按夹角去重；
    FF/FE 退化 facet 按 A、B 支撑集的仿射维数扣除，并报出 `facet_degenerate`
    （tetra vs box：严格 7 = 10 − 3，`tests/test_covers3_g0_vote2.py::test_vote2_accounts_for_degenerate_facets_tetra_box`；
    box vs box：0 = 6 − 6，`test_vote2_accounts_for_degenerate_facets_box_box`）。190/190 由
    `test_vote2_has_teeth_every_single_dropped_facet_is_caught` 钉住。
    另外**票三也必须改**（审查清单外的新缺陷）：旧票三比的是凸包三角网格的顶点，退化输入上它会把落在 facet 内部/棱上的点也算进去，
    于是退化输入上票三会红（修复者称"每个退化输入都红"；本次复核了两例：tetra vs box 三角网格 19 点 vs 真顶点 13，
    box vs box〔半宽 (1,1,1) 与 (2,0.5,1.5)〕26 vs 8），此前被成员谓词先崩溃、且测试只用一般位置而掩盖。
    现在比凸包**极点**：`tests/test_covers3_membership.py::test_g0_vote3_counts_only_extreme_points_on_degenerate_input`
    （13 由 Fraction 精确枚举独立给出；19 / 26 由本次对 `convex_hull_3d` 三角网格数顶点复核）。
- `tests/test_g2_bdda3d.py`：`UNSUPPORTED` 非空即红；`n_covers == len(mine)`；加"伪造 ee 入口必红"与"注入多余 EE 盖必红"两条牙。
- `tests/test_lp.py`：UNBOUNDED 分支用回收锥极射线 oracle（Fraction 精确）替代静默 return；报告里"12 组"改为如实计数。
- `tests/test_margin.py` Lipschitz：局部差商（h = 1e-3·δ）+ E 顶点处基点 + **紧性下界** `worst ≥ 0.9/δ`。已在 scratch 验证能抓四类错误实现。
  - **订正（2026-09-24）**："能抓四类"不成立：只靠局部差商的设计抓不到**局部反号**（x[0] > 0 处法向取反，
    相邻两点同侧、差商不变）。实现另加了**闭环扫描**（三个坐标平面上各绕 E 一圈、相邻点间距 ≤ 0.15 的闭环，反号处必有一对相邻点跨过去），局部差商 ∪ 闭环 ∪ 原门 66 个全局点对共同构成上界判据。
    四类错误实现（随机单位向量 / 常向量 / 径向 / 局部反号）全部报红：`tests/test_margin.py::test_lipschitz_gate_has_teeth`；
    原门 66 个全局点对自己的牙：`test_lipschitz_gate_global_pairs_have_their_own_teeth`。
- `tests/test_limit.py` 锥线性化：k 取 [5,6,9,10,14,18,22]（无 4 的倍数），断言偏安全 + 误差 ≤ 1−cos(π/k) + 大 k 更小；删"单调"。Patton 参数化加 (0.2,0.1)、(0.3,0.1)。
- `tests/test_readers_roundtrip.py`：换成块序交换不变、bdda_df/bdda3d 逐步键唯一；bdda3d 读取器面下标不再硬编码 −1。

**M0.e 堵盲区（六处）**
- **二维凹几何 G0 重跑**：`bb52_g0.analyze` 的投票改为暴力 `polygons_overlap`（与三维同款设计），重跑 1186；结果无论如何写进报告，README 的 "L1 二维 ✔" 按结果改写。
  - **订正（2026-09-24，做法）**：实际做法不是"把投票换个 oracle"，而是**撤掉投票**。真值由暴力 `polygons_overlap` 给出，
    盖这一侧检验**完备性**：分离样本上查距离完备性，相交样本上查出口完备性。重跑的是**同一批 1260 次平移**
    （21 块对 × 60）；1186 只是旧投票当初计入的那部分——在同一平移序列上重放旧投票恰好复现 1186/1186，其中暴力判相交的 621 次
    当时是靠假规则"通过"的（`tests/test_bb52_g1.py::test_g0_rerun_replays_the_original_1186_translations`）。
  - **订正（2026-09-24，结果：已完成）**：1260 次平移，暴力判相交 657 / 仅相触 0 / 分离 603；分离侧距离完备性 **603/603**，
    最大绝对误差实测 0（`reports/bb52_g1_g0.json` 的 `g0_worst_abs_error`；门只钉门限 1e-12：
    `test_g0_distance_completeness_real_concave_geometry`）。相交的 657 次起初无门可检（修复记录原写"留到 M3"），
    其后 36103be 补上出口完备性 **657/657**、最坏见证实测 1.8e-15（同一 JSON 的 `g0_exit_worst_witness`；门限 1e-12：
    `test_g0_exit_completeness_real_concave_geometry`，牙：`test_g0_exit_completeness_has_teeth`）。**仍未被门检验的**：凹块相交样本上的**成员判定**本身——要等入口块全局构造（M3）。
    机读结果：`reports/bb52_g1_g0.json`（77600c9 重新生成）。
- **首入**：README 与 `phase2_3d_kernel.md` 立即删掉"首入"；三维实现列入 M2。
- bdda3d 三个 JSON 进 `PROVENANCE.json` 并受 `test_fixture_provenance` 看护。
- 正本对代码：写 `docs/canon_map.md`——01 精细版命题 1–12、03 裕度四定理逐条对应到函数与测试；命题 4 的"一般位置"前提在代码里显式列出退化情形的处理。
  - **订正（2026-09-24，状态）**：`docs/canon_map.md` 已有一份草稿，与本批文档**一起提交**（HEAD 094861c 上尚不存在）；
    它尚未经 G6，内容是否到位由 G6 判定。
- tf 读取器：加执行门（TF_MODE 4/5/6 折叠的依据写明；census 数字由测试重算）。
- 端到端：`cb2_sliding` / `cb2_locked` 夹具 → `polyhedron_preserving_indices` → `enumerate_covers3` → `contacts_from_covers` → `limit_load`，
  断言 sliding 例的机构是平动、locked 例承载更高；bb52 二维几何走 kernel2d → 二维极限分析（需要 dim=2 路径接 2D 盖）。
  - **订正（2026-09-24）**：三维部分已做（b26d7f5），但两处断言改了：三个 bdda3d 夹具的块体几何相同，cb2_locked 与 cb2_sliding
    更是除 `case` 名外逐字段相同、且都不带荷载，"locked 承载更高"不是现有夹具能给出的判决；"机构是平动"只在对偶解唯一时才能断言，而对偶最优面不止一点（切向偏角可变，滑动时还含带转动的机构）。
    门对纸笔闭式核对承载力，绝对误差 ≤ 1e-9（尺度格 `test_capacity_ratio_is_invariant_to_units` 相对误差 ≤ 1e-9）；
    不是逐位相等（例如 μ = 0.3 时 α = 3.0000000000000018）。"locked 承载更高"挪到 M1，见 M1 新门的订正。
    破坏模式只读对偶最优面的不变量，8376ec2 起用独立的刚体虚功率核算判
    （`tests/test_end_to_end_cb2.py::test_failure_mode_is_read_off_invariants_of_the_dual_face`）。
    b26d7f5 版的门曾在滑动分支断言"无转动"，那不是不变量，8376ec2 已删。二维部分未做。详见下文"M0 执行状态"。

**M0.f 声称订正**：README 现状表、`phase2_3d_kernel.md`、`margin_layer.md`、`l3_limit_analysis.md`、`g2_three_dimensional.md` 按审查表逐条改。

**M0 退出判据：重跑六路审查，0 critical、0 major。** 不是"测试全绿"。

#### M0 执行状态（2026-09-24）

**结论：代码侧的修复已全部合并（main，HEAD 094861c；8376ec2 之后的 094861c 只改了 `src/eab/limit.py` 模块文档一行），
全套 1209 passed（主树与一份新鲜克隆各跑一遍）；但 M0 还没有退出——G6 重审尚未跑。**
（核对命令：`git clone --no-hardlinks D:/eab-kernel <scratch>` 后在克隆里 `python -X utf8 -m pytest -q -p no:cacheprovider`，
2026-09-24 在 8376ec2 实跑 1209 passed，在 094861c 主树与新鲜克隆再跑仍是 1209 passed；此前在 77600c9 是 1174 passed。
该克隆继承系统级 `core.autocrlf=true`（克隆里 `git config --get core.autocrlf` 读出 true），仍全绿——`.gitattributes` 的 `* -text` 起作用了。）

**已完成**
- 四组修复（g1-lp / g2-geom / g3-theory / g4-legacy）各在独立 worktree 里修，由独立验证者逐条核过代码，
  "被放松的断言"逐条裁决、必要时返工，然后按 g2-geom ddec237 → g1-lp b747c3b → g3-theory 822f0cd → g4-legacy b1bbd27 的顺序合并。
  M0.a–M0.d 各条都在其中；与原文不符的做法已在上面各条原位标了"订正（2026-09-24）"。
  例外：g4 组合并（b1bbd27）时，C17 的第二轮复核结论是 NO_TEETH，这一条是合并之后才由 6f157ac 补牙的（见下）。
- M0.e：二维凹几何 G0 重跑（见上文订正）；bdda3d 三个 JSON 进溯源（c1326a0，
  `tests/test_fixture_provenance.py::test_every_fixture_file_is_hash_gated`、`test_bdda3d_gate_has_teeth`）；
  tf 读取器执行门（da5691a，TF_MODE 4/5/6 改标 UNVERIFIED、6716 条 census 由 `tests/test_tf_reader.py` 重算）；
  三维端到端（b26d7f5 + 8376ec2，见下）。
- 四组修复记录之外，主干上还有六件（cc5d7cf 是四组的共同基线，其余五件在 b1bbd27 合并之后：
  `git log --oneline --no-merges b1bbd27..HEAD` 列出 6f157ac、b26d7f5、77600c9、8376ec2、094861c）。**后五件都是直接提交进 main 的，
  没有经过独立验证者复核**——下面写的"复核"都是本次文档收尾者自己复跑，不能替代 G6：
  1. **cc5d7cf 夹具溯源**——见第一节纪律 D。
  2. **6f157ac G2 带内牙穿过枚举器（C17）。** b1bbd27 合并时第二轮验证判 NO_TEETH：带内检测只在"枚举之后追加的假盖"上测过。
     本次复核（094861c 的新鲜克隆上，`tests/test_g2_bdda3d.py` 共 58 例）：只把 `tools/g2_bdda3d.py::compare()` 里
     `enumerate_covers3` 的实参改回 `window`、记下的 `enumerated_window` 不动 → 3 failed / 55 passed，红的正是新加的
     `test_g2_band_catches_a_real_kernel_defect_through_enumeration` 三例（cb2_locked / cb2_sliding / cb_bond；在内核里让
     `vertex_cone_contains` 恒返回 0）；若连 `ewin = max(window, band)` 这一行一起改回 → 22 failed / 36 passed
     （6f157ac 同时加的 `enumerated_window >= band` 断言也会响）；改回这一行、同时去掉该断言 → 又回到 3 failed / 55 passed。
  3. **b26d7f5 端到端门** `tests/test_end_to_end_cb2.py`（当时 113 例）：真夹具 → `polyhedron_preserving_indices` → `enumerate_covers3`
     → `contacts_from_covers` → `limit_load`，对纸笔闭式：滑动 μW、沿轴推倾覆 W、沿对角推倾覆 √2·W；
     几何 ×1e-3/×1e3 与荷载 ×1e-2…×1e4、接触点取 a / b / mid 三侧。写它时发现两件事：
     (a) **对偶机构不唯一**：八边形内接锥下推力恰落在锥棱上时，切向速度可在 ±π/k 内任取，承载力照样精确
     （求解器在沿轴滑动例实测返回偏 −22.5°；这是观察，不是门）；
     (b) **对称位形藏符号错**——见第一节纪律 E。
     分界格子：b26d7f5 只取到轴向分界 μ = 1；对角分界 μ = √2 当时只被 1.3 / 1.5 夹住，8376ec2 才把 μ = √2 本身加进参数化
     （`test_diagonal_push_uses_the_sqrt2_tipping_line`）。b26d7f5 提交说明里的 "boundary cells at mu = 1 and mu = sqrt(2)" 对 b26d7f5 本身只成立一半。
  4. **77600c9 重新生成机读报告**：`reports/bb52_g1_g0.json`（二维 G0：距离 603/603、出口 657/657）与
     `reports/tia_sensitivity.json`（剖面带可微标志）。只改这两个 JSON、不改代码或测试，同样直接进 main、未经独立复核。
     M0.e 订正里引用的 `g0_worst_abs_error = 0`、`g0_exit_worst_witness = 1.8e-15` 出自它。本次复核：在 8376ec2 上
     现算 `tools/bb52_g0.analyze(samples_per_pair=60, seed=0)`（即 `tests/test_bb52_g1.py` 的 `result` 夹具），
     1260 / 603 / 603 / 0.0 / 657 / 657 / 1.7763568394002505e-15 七个字段与该 JSON 逐字段相等（未重写 JSON）；
     TIA 剖面 19 行全部 `differentiable=False`，与 `test_phase_derivative_at_phase_zero_is_flagged_on_every_default_row` 一致。
  5. **8376ec2 端到端门返修**（现 115 例）。起因是本批文档核查者对 b26d7f5 的三处指认：
     - b26d7f5 的 `test_failure_mode_is_read_off_the_dual_mechanism` 在滑动分支断言"返回的机构无转动"，**那不是对偶最优面的不变量**：
       (vx, vy, vz, ωx, ωy, ωz) = (1, 0, μ, μc, 0, −c)，c = 1 − 1/√2（μ = 0.4 时 ≈ (1, 0, 0.4, 0.117, 0, −0.293)）
       在原问题上同样容许、上限同样等于 α = μW（本次用该文件的独立核算 `_power` 代入：μ = 0.4 / 0.95 最差接触功率 0 / −1.1e-16、
       上限 4.0 / 9.5 = μW），断言成立与否取决于单纯形挑哪个顶点。
       同理，b26d7f5 提交说明（及同文件 `test_capacity_is_independent_of_the_moment_reference_point` 的 docstring）里
       "力矩反号变异下本文件其余测试全绿"对 b26d7f5 **不成立**：参考点在对称中心时那个变异只是列的重排（同一个 LP），
       它当时弄红的 6 格里，除参考点不变性门的 4 格外，另 2 格（滑动 μ = 0.4 / 0.95）正是这条脆断言，不是抓到了缺陷
       （本次把 b26d7f5 版的该文件取出、同一变异复跑：6 failed, 107 passed）。
     - 该测试已删，换成 `test_failure_mode_is_read_off_invariants_of_the_dual_face`：用不调用 `eab.limit.wrench` 的独立刚体虚功率核算，
       断言返回的机构是最优对偶；滑动（μ < 1）时"平动 + 关联剪胀" T = (1,0,μ,0,0,0) 也是最优对偶；倾覆（μ > 1）时绕前缘转动
       R = (0,0,1,0,1,0) 是最优对偶，且任何纯平动的上限 ≥ μW > α——所以"带转动"只在倾覆时是真不变量；μ = 1 两者并列最优。
       **"滑动无转动"不再被断言**。
     - 本次复核（8376ec2 上，用把 `limit._build_columns` 接触列力矩取反 / 忘减参考点的两种变异各跑一遍整文件）：
       两种变异都只红 `test_capacity_is_independent_of_the_moment_reference_point` 的 4 格（2 个非零原点 × μ ∈ {0.3, 1.5}），
       其余 111 例全绿——即那句 docstring 在 8376ec2 上才恰好成立。
     - 新增 `tests/test_lp_fail_loud.py`（33 例）：钉住"要么答对、要么抛 RuntimeError、绝不静默给错"的契约
       （lp_tol ∈ {None, 1e-9, 1e-12, 1e-14} × 荷载/几何尺度 × k ∈ {5, 16}）；显式传紧容差时黏聚 ×1e4 算例会抛错，契约允许；
       默认容差下必须答出来（`test_default_tolerance_answers_the_audited_case`）。
  6. **094861c `src/eab/limit.py` 模块文档诚实条款**：只改一行 docstring，不改代码或测试。门改指
     `test_failure_mode_is_read_off_invariants_of_the_dual_face`；"是否转动"改写为只在倾覆（任何纯平动的上限都严格大于 α）时
     才是不变量，滑动时对偶最优面里既有纯平动 + 关联剪胀、也有带转动的机构（例 μ = 0.4 时 (1, 0, 0.4, 0.117, 0, −0.293)）。
     起因是本批文档核查者指出该条款仍引用已被 8376ec2 删除的测试名。本次复核：`grep -rn read_off_the_dual_mechanism --include=*.py`
     零命中；094861c 上全套 1209 passed（主树与新鲜克隆）。同样直接进 main、未经独立复核。

**还剩什么**
- **G6 六路重审没跑。** 这是 M0 唯一的退出判据；在它之前，上面每一条"已完成"都只是"修复者与验证者认为完成"，
  6f157ac / b26d7f5 / 77600c9 / 8376ec2 / 094861c 五件连独立验证者都没有。
- **正本对代码**（`docs/canon_map.md`）：草稿与本批文档一起提交（HEAD 094861c 上尚不存在），尚未经 G6，内容是否到位由 G6 判定。
- **端到端的二维部分**没做：bb52 二维几何 → kernel2d → 二维极限分析这条路径还不存在（`tests/test_limit*.py` 与
  `tests/test_end_to_end_cb2.py` 都不引用 kernel2d）。
- **"locked 例承载更高"没有、也不能照原文做**：cb2_locked / cb2_sliding / cb_bond 三个夹具的块体几何逐点相同
  （本次用 `tools/g2_bdda3d.load_case` 读三例比较顶点集，全等），sliding/locked 之分不在导出的几何里；
  端到端门改为对纸笔闭式核对承载力，绝对误差 ≤ 1e-9（尺度格相对误差 ≤ 1e-9；见上）。
  这项判决**挪到 M1**（见 M1 新门的订正），前提是先有带荷载或状态差异的新夹具。
- **首入删改（M0.e）与声称订正（M0.f）**：README 与 `reports/` 的文档订正与本文件同批进行、尚未提交；到位与否由 G6 判定。
- **三维相交样本仍无门**：`kernel3d/g03.py::g0_distance_completeness3` 遇 `polyhedra_overlap != -1` 直接跳过（计入 `skipped_intersecting`）。
  二维那边已补出口完备性，三维的对应物还没有（g4-legacy 修复者提出，未做）。
- **交给 G6 重点看的清单外新发现**（都已修或已入门，但没经过 G6）：
  LP 在尺度格子上另有三处缺陷——荷载放大且带黏聚力时返回残差很大的假 optimal、列放大 1e2/1e4 时假 infeasible、
  一阶段舍入噪声被当成不可行（g1-lp 修复，门在 `tests/test_lp_scale.py`、`tests/test_limit_scale.py`；
  修后的"答对或抛错"契约由 8376ec2 的 `tests/test_lp_fail_loud.py` 钉住）；
  G0 票三在退化输入上会红（修复者称每例都红，本次复核 2 例；见 M0.d 订正）；夹具 LF 规范化（纪律 D）；C17 无牙（上文）；
  对偶机构不唯一、b26d7f5 的"滑动无转动"脆断言（上文第 5 件）。
- **`src/eab/limit.py` 模块文档的诚实条款已由 094861c 跟上 8376ec2**（上文第 6 件）：门改指
  `test_failure_mode_is_read_off_invariants_of_the_dual_face`，"是否转动"改为只在倾覆时才是不变量；
  `grep -rn read_off_the_dual_mechanism --include=*.py` 现零命中。未经独立复核，G6 核。

### M1 · 多体极限分析

把各体自由度并进同一组平衡方程（每体一个旋量块，接触在两体间成对出现，符号相反）。单体是它的特例，现有门直接继承。
新门：两块叠放的解析解；cb2 夹具的 sliding/locked 判决；三块拱的推力线；对偶机构容许性在多体下成立。
- **订正（2026-09-24）**："cb2 夹具的 sliding/locked 判决"用现有夹具做不了：`fixtures/bdda3d/` 的 cb2_locked 与 cb2_sliding
  两份 JSON 逐字段比较，只有 `case` 名不同（几何与入口节理参数全同），导出里也没有荷载/驱动字段。
  这项判决（即 M0.e 端到端原定的"locked 例承载更高"，已挪到这里）的**前提**是先重新导出带荷载或状态差异的夹具；
  没有新夹具就不是一道能写的门。
**这是护面块体与真实互锁结构的前提。**

### M2 · 首入（三维）+ 退化盖合并

首入是 DDA 真正消费的那条规则；没有它，DDA 适配器替换不了检测层。实现命题 7 的三维版（集值点上的确定性裁决），
并把 FF / FE / 平行 EE 退化族收成一个盖，同时保留"展开成 4 个 VF"的视图给 DDA。
门：G2 扩展到比较**入口选择**而不只是集合；退化族合并前后 legacy 的 4 个 np 入口能被还原。

### M3 · 凹块全局入口块构造

消除暴力谓词——目前凹块的逃逸高度与成员判定都还靠它裁决。研究级；G0 仍是唯一 oracle（且按纪律 A，oracle 不得用盖枚举的原语）。
门：互锁块、L 块、Xbloc 公开几何上，全局构造 vs 暴力谓词零失配；逃逸高度不再调用谓词而结果不变。

### M4 · b-DDA 接缝

bdda2 加 `BDDA_DETECT=legacy|eab`，照 `BDDA_SOLVER` 先例，15 例家族影子运行，T1 型同构门。切换由所有者决定。
前提：M0 全过、M2 首入落地（否则接触点与法向的裁决对不上 legacy）。

### M5 · SE(3)

出岩石最硬的一道坎：转动下 E 不再是 Minkowski 差。先做 SE(2)（绕固定轴转动，边界变曲线），对暴力谓词做门；
再推 SE(3)。周期未知，**不承诺**。

### M6 · 第二个域适配器

护面块体（凹互锁 = 产品本身，三维内核最富出口）或二维 caging。进来之前先搬两处层次违规
（`interlock.py` 出内核目录；契约层的引擎状态码映射表出契约）。域必须先证明自己过了 2×2 判据。

### M7 · C++20 `eab2`

只在撞到真实性能墙时做；复用 bdda2 的 `Scalar` / `Dual<W>` / `check_layering`。

## 三、这份计划自己的诚实条款

- M0 的规模是 31 条修法 + 6 处盲区 + 一次重审，**重审可能再抓出新的**；M0 结束的标志是重审干净，不是清单打完。
- 首入与凹块全局构造（M2、M3）是研究级，可能证明某条路走不通；走不通也是结果，照样写报告。
- 跨层交叉验证（L3 == L5）是目前唯一经受住审查的强验证；以后每个新层都要找到它的"零共用代码的另一条路"，找不到就承认那一层没有硬验证。
- 上一版计划在两天内被推翻五条；这一版没有理由更耐久。**每个里程碑结束重读一次这份文件。**
