# 独立审查（2026-09-21）：268 个绿灯底下有什么

六路独立审查（LP/极限分析、三维几何、理论-代码一致性、二维与读取器、门的牙、声称对照代码），
**每条发现都被另一路用代码实际复现或推翻**，最后一路专门回答"什么没审到"。
40 个智能体、413 次工具调用、25 分钟。

**35 条原始发现 → 去重 33 → 31 条确认、2 条推翻、0 条存疑；另有 6 处审查盲区。**
确认的 31 条里：**2 条 critical、15 条 major、14 条 minor。**

先说结论：**过去三天写的代码里有真正给出错误数值的缺陷，268 个测试一条都没抓到；
README 与报告里有四条声称是假的；两条地基性的结论（二维凹几何 G0、首入）缺乏支撑。**
L3 极限分析与 L5 逃逸剖面那次跨层交叉验证仍然成立——它之所以成立，恰恰是因为两条路径零共用代码。
这次审查最大的教训就在这句话里。

## 为什么 268 个绿灯没抓到

审查把原因看得很清楚，三条都是结构性的：

1. **参数化恰好避开了失败格子。** 极限分析的 Patton 门只测了 (amp, μ) 六组，而 (0.2, 0.1)、(0.3, 0.1) 会静默返回 `infeasible`；
   裕度层的可微路径只在一个手选偏移上测，而 `body_distance` 在一般位置下有 51% 的概率返回该路径不支持的标签；
   逃逸高度只在 nx=4 的对称块上测，换 nx=5 就 10/25 错。
2. **oracle 与被测代码共用同一段几何原语。** `point_in_polyhedron`（射线奇偶）与"暴力"谓词都用从 f[0] 出发的扇形三角化，
   非凸面上两者一起错；`_point_inside_face` 与 `_point_face_distance` 都用半平面核（只对凸面正确）；
   二维 bb52 的"凹几何 G0"用 sign(min gap) 当 oracle——**正是我在三维上亲手证伪过的那条规则**。
3. **声称先于门写进了文档。** "三维 G0 三票"实际只实现两票；"首入"三维没有实现；"随 k 单调收敛"不单调；
   "贯入之后见证距离恒为 0"是假的——贯入时 `body_distance` 返回一个**正的**假距离，裕度谓词回答"安全"。

## 确认的缺陷（按严重度）

### Critical

**C1 · `lp.py:100` + `limit.py:171` — 可行问题被静默判成不可行。**
一阶段可行性用绝对阈值 `-T[m][-1] > tol`，而 `limit_load` 把 tol 再乘 1e-3 变成 1e-12；
b = −W_dead 量级 100 时，一阶段收敛后人工变量和的舍入残差 ~2e-12，于是返回 `status='infeasible', alpha=nan`。
实测：`interlocking_pair(4,2,0.2)`、μ=0.1、默认 k=8 → infeasible（真值 52.083）；
(0.25,0.2) 在 k = 6/7/8/19/20/32 上 optimal/infeasible 交替；测试自己的 k=16 下 (0.2,0.1)、(0.3,0.1) 都 infeasible。
**测试套件是靠参数化恰好避开这些格子才绿的。** 相对阈值（tol × max(1, max b)）或行归一化后三例全部 optimal 且等于 Patton 解析值。

**C2 · `covers3.py:446` — 凸成员谓词丢掉全部退化 facet，给出错误判定并会崩溃。**
`membership_convex3` 只用 `strict=True` 的 facet 取 max。凡是 B 的面法向恰与 A 的某面反平行、或与对方棱平行的 facet 都非严格，被整个丢掉。
实测：四面体 vs 单位方块，平移 x=(t,0,0) 在 t ≥ 1.0 时全部判"相交"（t=1.5/2/3 明明分离 ≥ 0.5）；
`g0_convex3(tetra, box)` 报 57/200 失配；两个轴对齐方块直接崩溃（空集取 max）。
随机凸包处于一般位置所以没有退化 facet，G0 门因此一直绿。
修法：成员谓词改用 E = B ⊕ (−A) 的**全部支撑半空间**（对 B 每个面法向 n 取 A 上 n·a 最小的顶点，反之亦然），与严格性无关。

> **订正（2026-09-24）：上句的"反之亦然"要说准，JSON 里给的代码片段在这里是反号的。** 对 A 的每个面法向 m，
> 应取 B 上使 **m·b 最小**的顶点（E 在这里的 facet 法向是 −m），间隙为 (−m)·(a_f + x − b*)。
> `audit_20260921.json` 的 `confirmed[5].fix` 写成了 `max(..., key=lambda i: dot(n, B.verts[i]))`（取最大），照抄会错：
> 修复者记录说该片段在 273c984 让 26 个门变红。我把它原样打进 `support_gaps3` 复跑全套：在 273c984 的新鲜克隆上得
> 31 failed / 480 passed，在主干 8376ec2 上得 31 failed / 1178 passed。两处都比 26 多出同样 5 个，即
> `test_covers3_membership.py::test_support_gaps_are_supporting_half_spaces[0-4]`（直接测 `support_gaps3`）。这条测试
> **不是后来新增的**，它和修复一起在 273c984 加入（`git log -S test_support_gaps_are_supporting_half_spaces -- tests/` 只有 273c984）。
> 所以修复者的"26"要么没把这 5 个算进去，要么是写这条测试之前数的；记录里没说是哪一种。其余 26 个与修复者所说一致，
> 包括六组随机对 G0（`test_g0_convex3_random_pairs`）、方块/方块、四面体/方块。现行实现见 `covers3.support_gaps3`
> （取最小，正确）；勘误已记入 JSON 顶层的 `errata`。

### Major

**M1 · `limit.py:239` — `check_duality` 在有黏聚力时全错。** 两个独立原因：列表只由摩擦锥棱重建而 `res.lambdas` 里黏聚列交错其间，下标错位（复现出 0.3333 的假互补违约）；对偶解里黏聚约束行的乘子被 zip 截断，上限漏掉 Σy_row，对偶间隙恰等于黏聚贡献（复现 6.0）。测试从未在有黏聚力时调 `check_duality`。修法：把列构造抽成 `_build_columns` 三处共用。

**M2 · `covers3.py:223` + `geom3.py:475` — 点在面内的判定用的是半平面核，不是多边形内部。** 对有反射顶点的面（L 块顶底面、`merge_coplanar` 归并出的十边形侧面），投影落在"臂"里的顶点被判 `in_extent=False`，VF/FV 盖被丢，`brute_feature_distance` 退化到顶点/棱对而高估距离。修法：统一一个 `point_in_face_polygon`（投影到主轴平面 + 环绕数），三处共用。

**M3 · `g03.py:50` — "三维 G0 三票"实际只有两票。** `local_facets3` 被算出来但从未与 conv{b−a} 的 facet 集比较；`passed` 不看 facet。删掉任一严格 facet，181/190 次仍 passed。文档与 `reports/phase2_3d_kernel.md:16` 的声称是假的。

**M4 · `geom3.py:394` — 扇形三角化只对凸面正确。** `_point_on_face`、`segment_crosses_face`、射线奇偶三处都从 f[0] 扇出并接受任一三角命中，非凸面上三角溢出多边形。实测：细杆 vs L 块分离却报 1；点被判"边界"。与 M2 同一个修法。

**M5 · `geom3.py:449` — 暴力相交谓词仍会漏判正体积交集。** 每个顶点都落在对方面平面上、且无棱穿面内部时（L 块凹槽里咬进 ~0.05 的方块），前三步证人全空，兜底网格采样的是 AABB 交集（2×2×1）而非真交集（0.003×2×1），返回 0（相触）而真值是 1（体积 0.012）。修法：对每个"相触"顶点沿内法向和微推做证人。

**M6 · `geom3.py:632` — `merge_coplanar` 没有邻接判定。** 只按支撑平面分组：两个共面面只在一个顶点相触（pinch）时 `boundary[a]=b` 字典静默覆盖、`while cur != start` **死循环**；两个共面但不共棱的面（U 形棱柱两个端面）被强行归并后抛 ValueError。两种都是合法闭流形。修法：平面组内按共棱做并查集连通分量再归并。

**M7 · `interlock.py:92` — 逃逸候选集不完备，文档撒谎。** docstring 说"不施加 in_extent 过滤"，代码却用 `enumerate_covers3` 的默认 `require_in_extent=True`。默认块（nx=4，对称）上 0/25 错，`interlocking_pair(5,2,0.3)` 上 **10/25 随机偏移错**（0.44749 vs 真值 0.40054，静默偏大）。

**M8 · `margin.py:46` / `:118` — 裕度层贯入时给出"安全"，是 fail-unsafe。** `body_distance` 与 `margin_contacts` 没有 overlap 守卫；贯入时 |point_a − point_b| 是贯入深度（正数），`margin_contacts(delta=0.1)` 返回 []（"无广义接触闭合"），而同一输入 `inflated_membership` 答 INSIDE。模块文档与 `reports/margin_layer.md:81` 说的"贯入之后见证距离恒为 0"是假的。修法：入口处 `polyhedra_overlap==1` 则返回哨兵接触（margin_gap = −δ、closed=True）或抛错。

**M9 · `margin.py:175` + `frozen.py` — 可微裕度路径对约一半分离位形崩溃。** `body_distance` 在一般位置下 51% 返回 VE3/EV3 标签（400 样本：134 + 63），而 `frozen_normal`/`frozen_gap` 只支持 VF/FV/EE/VV3，直接 raise。测试 `assert lab[0] in (...)` 把缺失分支当前置条件。补两个泛型分支（顶点到棱直线的投影残差）后 400 样本零崩溃、maxerr 8.9e-16。

**M10 · `tools/g2_bdda3d.py:83` — G2 门对棱-棱接触结构性失明。** legacy 非 np 入口进 UNSUPPORTED 桶却不导致失败；内核 EE/VE3/EV3/VV3 盖进 detail 桶不参与对账。实测：伪造一条 `ee` legacy 入口 → 门绿；注入一个多余的内核 EE 盖 → 门绿。修法：UNSUPPORTED 非空即红；`n_covers == len(mine)` 否则红。

**M11 · `tools/tia_sensitivity.py:62` — 在不可微点报出一个导数。** 默认剖面 phase=0 处 h 关于 phase 是偶函数（h(δ,+φ)=h(δ,−φ)>h(δ,0)，已对暴力二分核实），∂h/∂phase 不存在；报出的 +0.5δ 是 float 路径恰好选中的那个并列盖的**单侧**方向导数，选左邻会得到另一个标签。tan_psi 在盖切换点（δ=0.5）同理。修法：冻结后在 ±ε 重定盖标签，标签变化或单侧差商不一致即标记 `differentiable=False`。

（M12–M15 为上述条目的重复报告，已合并计数。）

### Minor（14 条，逐条见 `reports/audit_20260921.json`）

要点：三维内接锥"随 k 单调收敛"**不成立**（k 为 4 的倍数时棱恰落在滑动方向所以精确，其余 k 振荡；门里 k=6 被 `if k>=8` 豁免，"converges"从未被检验）；
`contacts_from_covers` 取见证段中点当接触点，gap>0 时力的作用线偏移 gap/2，倾覆算例 3.1746 vs 真值 3.3333（应取 `point_a`）；
`test_lp` 在 UNBOUNDED 时静默 return，**7/12 个种子走这条路**，"对枚举全部基本可行解（12 组）"实际只有 5 组；
`Dual.__float__` 让 `math.sin(Dual)` 静默降为 float，而 `osteomorphic_block_generic` 默认 `sin_fn=math.sin`——不传 `sin_fn=D.sin` 时 ∂h/∂phase 从 0.23 静默变 0，primal 逐位不变，任何值门抓不到；
`atan2` 两个 Dual 宽度不同时静默截断；`__pow__` 负底非整数幂返回复数；
二维 `normal_cone` 遇连续重复顶点抛 ZeroDivisionError，读取器只剥尾部回绕点不剥内部重复；
`ve3_cover`/`ev3_cover` 把 `strict` 写死为 True，低维盖的 strict 过滤是死代码；
`escape_height_covers(verify=False)` 返回的正是文档判定无效的纯盖规则（δ=0.4 → 0.300 vs 0.200），无测试无警告；
`test_inflated_normal_is_lipschitz` **无牙**：样本点对间距 0.42 ≫ δ，任何单位向量函数的商都 ≤ 4.81 < 8，随机向量/常向量/局部反号四种错误实现全过；
`test_readers_roundtrip` 的 `len(keys)==len(records)` 是同义反复，且 `canonical_key` 对 bdda3d 不单射（面下标硬编码 −1）；
`polyhedron_preserving_indices` 未被任何面引用的输入顶点静默保留。

## 推翻的 2 条

- `bdda_df.py:156` VV 接触的 feature_b 标成 'edge'：复现者用 verts.csv 实测了三个真实案例，legacy 的 v-v 行本身就是"顶点对边 (p2,p3) + 第二参考对边 (m5,m6)"的组合，真正配对的顶点是两条边的公共端点（三例里两次是 p3 一次是 p2，p2 距 p1 可达 1.4）。**把 p2 标成 edge 是忠实转写**，改成 vertex 反而 3 例错 2。
- `tf_probe.py:76` 探针 contact_type 被当入口类：`CoverType.NN/NE` 自带"候选类（入口展开前）"语义，raw 码保留，1420 行全是 2（n-p），两种读法下都是 VF；area=1.0 的占位性质台账已写明。数字复现但无一是错值。

## 六处审查盲区（比缺陷更要紧）

1. **二维凹几何 G0 "1186/1186" 建在已证伪的规则上。** `tools/bb52_g0.py:135` 用 sign(min gap) 判相交——正是三维上实测被推翻的那条。批评者构造了细臂 L 块 + 凹槽内悬浮方块：`polygons_overlap` 说分离，投票说相交。README "L1 二维 ✔" 的凹块部分**目前无支撑**，必须换成暴力 `polygons_overlap` 重跑。
   **重跑结果（2026-09-24）**：同一批平移（21 块对 × 60 = 1260 次），真值一律暴力 `polygons_overlap`——分离 603 次，
   距离完备性 603/603（误差 0）；相交 657 次，出口完备性 657/657（最坏见证距离 1.8e-15）；仅接触 0 次。
   旧规则不止会误报：它在相交方向也会漏（槽角楔块，计入 53 次漏报 15 次）；细臂 L 块上误报 11/60。
   重放旧规则仍得 1186/1186，其中 621 次是相交样本。凹块**成员判定**本身仍无门（里程碑 M3）。
   依据：`tests/test_bb52_g1.py` 的 `test_g0_distance_completeness_real_concave_geometry`、`test_g0_exit_completeness_real_concave_geometry`、
   `test_g0_rerun_replays_the_original_1186_translations`、`test_g0_gate_no_false_alarm_on_thin_arm_L_notch`、`test_g0_gate_no_false_negative_on_wedge_in_channel`。
2. **首入（命题 7）三维零实现。** grep `first_entrance|首入` 在 kernel3d 下无结果，而 `reports/phase2_3d_kernel.md:13` 与 README L1 都把"首入"列为已交付。首入恰恰是 DDA 真正消费的那条规则（集值点上谁定接触点与法向）。二维实现对凹块分离情形也承认不对。
3. **G2 的 legacy 侧未被哈希看护。** `fixtures/PROVENANCE.json` 里 bdda3d 条目为 0，三个 JSON 若被编辑或换版本重导，"零差异"会静默失效。
4. **没有做过"正本对代码"的核对。** `D:\E(A, B)\01 精细版` 的命题 4 明写四类盖清单只在**一般位置**下成立——这次的方块崩溃与四面体误判正是退化情形；自创的"定理"（裕度层四条）也没人对照过正本编号。
5. **tf.cpp 读取器路径从未被执行攻击。** TF_MODE 把状态 4/5/6 折进 SLIDING/LOCKED 无验证。
6. **没有任何跨层端到端路径被测过。** `limit_load` 从未跑过 cb2_sliding/cb2_locked 夹具（名字里带着免费的力学判决）；接缝处（非零窗口的 `contacts_from_covers`、裕度→极限、夹具几何→盖→LP）正是这次发现静默错值的地方。

## 对既有声称的订正

| 声称 | 实际 |
|---|---|
| 三维 G0 三票 | 两票；票二未实现 |
| 二维凹几何近接触 G0 1186/1186 | oracle 是 sign(min gap)，规则已证伪，**撤回待重跑** |
| ↳ 重跑结果（2026-09-24） | 真值改暴力 `polygons_overlap`：分离 603，距离完备性 603/603；相交 657，出口完备性 657/657。旧规则在相交方向也错（槽角楔块 53 次漏报 15 次）。凹块成员判定仍无门（里程碑 M3） |
| 三维盖层含首入 | 三维零实现 |
| G2 对 bdda3d 零差异 | 仅 VF 类、一种几何、legacy 侧未哈希看护 |
| 裕度层贯入时只能答"已违约" | 实际答"安全"，fail-unsafe |
| 三维锥线性化随 k 单调收敛 | 不单调；k 为 4 的倍数时精确 |
| Patton 律六组全中 | 六组是对的，但求解器在相邻格子静默 infeasible |
| L3 承载比 == L5 剪胀比（跨层交叉验证） | **仍成立**，两条路径零共用代码 |

完整机器可读结果：`reports/audit_20260921.json`（31 条确认各带复现脚本与修法、2 条推翻、6 处盲区、六路覆盖说明）。

## M0 处置结果（2026-09-24）

**先说结论。** 31 条确认缺陷全部修了；其中 30 条经独立复核者判 VERIFIED（在回退了修复的代码上门变红、在修复后变绿）。
C17 不一样：g4 组合并（b1bbd27）时它的第二轮复核判"无牙"（NO_TEETH），6f157ac 补了牙，但 6f157ac 是合并之后直接进 main 的，
**没有经过独立复核者**；补牙只由补牙者本人和本次文档核对用同一变异确认过会红。
六处盲区里堵上 3 处（GAP0、GAP2、GAP4），部分堵上 1 处（GAP5，同样直接进 main、未经独立复核），仍开放 2 处（GAP1 首入、GAP3 正本对代码）。
M0 期间另发现 5 件审查没抓到的事，都已修（见本节末）。
测试基线 8376ec2 全套 **1209 passed**（审查时 268；77600c9 时 1174），在主树和一份新鲜克隆里各跑了一次
（`python -X utf8 -m pytest -q -p no:cacheprovider`）。main 现为 094861c：其后只改 `src/eab/limit.py` 第 41 行模块文档
（诚实条款改指 `test_failure_mode_is_read_off_invariants_of_the_dual_face`，见本节末第 5 条），不动代码与测试；
在 094861c 上主树与新鲜克隆全套仍 1209 passed。
b1bbd27 之后主干上 6f157ac、b26d7f5、77600c9、8376ec2、094861c 五件都是直接进 main 的，**都没有经过独立验证者复核**
（`git log --oneline --no-merges b1bbd27..094861c`）。**但 M0 的退出判据是 G6：重跑六路审查、0 critical 0 major，
这一步还没做——所以 M0 还没有结束，本节的任何说法都没有经过 G6。**

**怎么读这张表。** "编号"是 `audit_20260921.json` 里 `confirmed[i]` 的下标 Ci、`gaps[j]` 的下标 GAPj（修复与复核记录都用这套编号）；
"本文"是本文上面用的 C1/C2/M1–M11 编号——本文把 4 条重复报告合并了（M12–M15），所以两套编号对不齐，这里给出对照。
（下文"里程碑 M2/M3"指 `docs/plan_20260921.md` 的里程碑，不是本文的缺陷编号 M2/M3。）
"状态"：**verified** = 修了，且独立复核者在回退修复的代码上看到门红、在修复后看到门绿；**fixed** = 修了，但最后一轮复核
不是独立完成的；**部分** / **开放** 顾名思义。每行列出的测试都在测试基线 8376ec2 上，且都在那 1209 个里全绿（094861c 上同样）。
个别复核附带说明，见修复记录，不影响判定：修复者为 C0 列出的 `test_limit_scale.py::test_slide_topple_closed_form_holds_across_units`
在回退代码上也绿（只是对照，对这条缺陷无牙）；为 C0、C21 列出的 `test_limit_scale.py::test_old_tight_lp_tolerance_no_longer_breaks_feasibility`
在回退代码上只因 `TypeError`（旧代码没有 `lp_tol` 参数）而红，复核者明言**不是真红**。所以下表不拿这两条当代表性看护测试；
C21 判 VERIFIED 靠的是 C21 行列出的三条（复核记录：回退代码上真红）。

### 31 条确认缺陷

| 编号 | 本文 | 缺陷（一句话） | 修复提交 | 状态 | 代表性看护测试 |
|---|---|---|---|---|---|
| C0 | C1 | LP 一阶段用绝对阈值，可行问题被判 infeasible | b754407 | verified | `test_limit_scale.py::test_interlock_is_never_falsely_infeasible_on_the_audited_cells`、`::test_plan_regression_cells_equal_patton`、`test_lp_scale.py::test_status_is_invariant_to_rhs_scale_even_at_a_tight_tolerance` |
| C1 | M1 | `check_duality` 在有黏聚力时列错位、上限漏项 | b754407 | verified | `test_limit_duality.py::test_pure_cohesion_duality_gap_and_complementarity_vanish`、`::test_friction_plus_cohesion_sliding_duality` |
| C2 | Minor | `test_lp` 遇 UNBOUNDED 静默 return | b754407 | verified（一处测试改动被列为疑似放松断言，复议判不是放松，第二轮 VERIFIED） | `test_lp.py::test_against_brute_force_basic_feasible_solutions`、`::test_unbounded_oracle_has_teeth` |
| C3 | Minor | 三维内接锥"随 k 单调收敛"不成立 | b754407、1fedb77 | verified（第二轮：恢复了旧的 k≥8 绝对下限） | `test_limit.py::test_three_d_cone_linearisation_is_conservative_within_the_inradius_bound`、`::test_three_d_cone_error_depends_on_frame_alignment_not_monotone_in_k` |
| C4 | Minor | 接触点取见证段中点，gap>0 时力线偏 gap/2 | b754407 | verified | `test_limit_contacts.py::test_toppling_capacity_is_independent_of_the_gap` |
| C5 | C2 | 凸成员谓词丢退化 facet，误判并崩溃 | 273c984 | verified | `test_covers3_membership.py::test_membership_tetra_vs_box_sweep_along_x`、`::test_membership_axis_aligned_boxes_does_not_crash`、`::test_g0_convex3_box_vs_box_runs_and_passes` |
| C6 | M4 | 扇形三角化只对凸面正确 | 273c984 | verified | `test_geom3_face_polygon.py::test_all_five_call_sites_route_through_the_single_helper`、`::test_rod_through_concave_notch_is_separated` |
| C7 | M2 | 点在面内用半平面核，凹面上丢 VF/FV 盖 | 273c984 | verified | `test_geom3_face_polygon.py::test_brute_distance_and_vf_cover_over_the_L_arm`、`::test_probe_in_front_of_merged_concave_side_face_has_a_face_cover` |
| C8 | M6 | `merge_coplanar` 无邻接判定：pinch 死循环、U 形端面误并 | 273c984 | verified | `test_geom3_merge.py::test_vertex_pinched_coplanar_faces_do_not_hang`、`::test_u_prism_prong_ends_stay_separate` |
| C9 | M5 | 暴力相交谓词漏判薄咬合 | 273c984 | verified | `test_geom3_overlap_witness.py::test_thin_notch_bite_is_detected`、`::test_audit_case_volume_is_0_012` |
| C10 | Minor | VE3/EV3 的 strict 写死 True | 273c984 | verified | `test_covers3_strict_lowdim.py::test_parallel_edge_edge_nearest_pair_ve3_is_boundary_state`、`::test_distance_completeness_does_not_depend_on_strict` |
| C11 | M9 | 可微裕度路径对 VE3/EV3 标签崩溃 | f52551a | verified | `test_frozen_lowdim.py::test_every_body_distance_label_enters_the_frozen_path_convex_400` |
| C12 | M7 | 逃逸候选集用了 in_extent 过滤，非对称块上偏大 | f52551a | verified | `test_interlock_escape.py::test_escape_height_covers_is_complete_off_axis_on_asymmetric_blocks` |
| C13 | M11 | TIA 在不可微点报导数 | f52551a | verified | `test_interlock_sensitivity.py::test_phase_derivative_at_phase_zero_is_flagged_on_every_default_row`、`::test_differentiability_flag_agrees_with_one_sided_quotients` |
| C14 | M8 | 裕度层贯入时答"安全"（fail-unsafe） | f52551a | verified | `test_margin_penetration.py::test_penetration_is_reported_as_a_violated_margin` |
| C15 | Minor | `escape_height_covers(verify=False)` 返回文档自认无效的规则 | f52551a | verified | `test_interlock_escape.py::test_unverified_escape_branch_is_gone` |
| C16 | Minor | `atan2` 宽度不同静默截断；负底非整数幂返回复数 | f52551a | verified | `test_dual.py::test_atan2_width_mismatch_is_an_error`、`::test_pow_of_a_negative_base_to_a_fractional_power_is_an_error` |
| C17 | M10 | G2 门对棱-棱与非 VF 类结构性失明 | 53ac9c1、36103be、6f157ac | **fixed**（第一轮 VERIFIED；恢复 VF/FV 带后，b1bbd27 合并时第二轮判 NO_TEETH；6f157ac 补穿过枚举器的牙、合并后直接进 main，未经独立复核者复核；补牙者与本次文档核对用同一变异确认会红） | `test_g2_bdda3d.py::test_g2_gate_red_on_fabricated_ee_legacy_entrance`、`::test_g2_gate_red_on_extra_kernel_ee_cover`、`::test_g2_band_catches_a_real_kernel_defect_through_enumeration` |
| C18 | Minor | 非凸包角点的输入顶点被静默保留 | 53ac9c1 | verified | `test_g2_bdda3d.py::test_g2_duplicate_vertices_are_aliased_and_reconcile`、`::test_g2_non_corner_vertex_is_a_clear_error` |
| C19 | Minor | 二维零长边抛 ZeroDivisionError；读取器只剥尾部重复点 | 96c6379 | verified | `test_kernel2d_review.py::test_c19_zero_length_edge_is_a_clear_error_not_zerodivision`、`::test_c19_loaders_strip_every_cyclic_consecutive_duplicate` |
| C20 | Minor | 读取器门同义反复；bdda3d 键不单射 | a87d47e、36103be | verified（删掉的同义反复断言被列为疑似放松，复议判不是放松，第二轮 VERIFIED） | `test_contact_record.py::test_c20_bdda3d_np_entrances_on_different_triangles_have_distinct_keys`、`test_readers_roundtrip.py::test_reader_key_gate_has_teeth_where_the_retired_assert_had_none` |
| C21 | （并入 C1） | 同 C0，门质量视角：`limit_load` 把 tol 再乘 1e-3 | b754407 | verified | `test_limit_scale.py::test_interlock_is_never_falsely_infeasible_on_the_audited_cells`（回退代码上 9/9 红）、`::test_patton_sweep_is_optimal_and_inside_the_inscribed_cone_bracket`（3 格红）、`test_limit.py::test_three_d_cone_linearisation_is_conservative_within_the_inradius_bound[7]`（红）。修复者原列的 `test_limit_scale.py::test_old_tight_lp_tolerance_no_longer_breaks_feasibility` 在回退代码上只因 `TypeError` 红，不算牙 |
| C22 | （并入 Minor） | 同 C2，门质量视角 | b754407 | verified | `test_lp.py::test_random_gate_exercises_both_branches_as_counted` |
| C23 | （并入 M3） | 同 C27，门质量视角 | 273c984 | verified | `test_covers3_g0_vote2.py::test_vote2_has_teeth_every_single_dropped_facet_is_caught` |
| C24 | （并入 M1） | 同 C1，门质量视角 | b754407 | verified | `test_limit_duality.py::test_check_duality_rejects_a_mechanism_that_ignores_cohesion_rows` |
| C25 | Minor | Lipschitz 门无牙 | f52551a、1529681 | verified（第二轮：容差恢复 1e-9、恢复 66 个全局点对） | `test_margin.py::test_inflated_normal_is_lipschitz`、`::test_lipschitz_gate_has_teeth`、`::test_lipschitz_gate_global_pairs_have_their_own_teeth` |
| C26 | （并入 Minor） | 同 C3，门质量视角：k=6 被豁免 | b754407 | verified | `test_limit.py::test_three_d_cone_bound_is_tight_for_loads_in_every_direction` |
| C27 | M3 | "三维 G0 三票"实为两票 | 273c984 | verified | `test_covers3_g0_vote2.py::test_vote2_local_facets_equal_hull_facets_on_random_pairs`、`::test_vote2_has_teeth_a_spurious_label_is_caught` |
| C28 | （并入 M8） | 同 C14，声称视角 | f52551a | verified | `test_margin.py::test_theorem_two_gate_has_teeth_shrinking_the_nearest_point_is_caught` |
| C29 | Minor | "单调"声称为假 | b754407 | verified（"单调"字样删除、不恢复——复议判不是放松） | `test_limit.py::test_three_d_cone_error_depends_on_frame_alignment_not_monotone_in_k` |
| C30 | Minor | `Dual.__float__` 让导数静默丢失 | f52551a | verified | `test_dual.py::test_implicit_float_conversion_of_a_dual_is_a_type_error`、`test_interlock_sensitivity.py::test_generic_block_default_sin_keeps_the_phase_derivative` |

### 六处盲区

| 编号 | 盲区 | 状态 | 依据 |
|---|---|---|---|
| GAP0 | 二维凹几何 G0 建在 sign(min gap) 上 | verified（42d8c1b、36103be） | 见上文盲区 1 的重跑结果；被删的旧断言复核判"两半都是假的、删除正当"，相交样本改由出口完备性看护 |
| GAP1 | 三维首入零实现 | **开放 → 里程碑 M2** | `grep -rn "首入\|first_entrance" src/eab/kernel3d` 仍无结果；`reports/phase2_3d_kernel.md` 已标撤回 |
| GAP2 | G2 的 legacy 侧未哈希看护 | verified（c1326a0） | `test_fixture_provenance.py::test_every_fixture_file_is_hash_gated`、`::test_bdda3d_gate_has_teeth`；61 个夹具文件全部入账 |
| GAP3 | 没做"正本对代码"核对 | **开放（待 G6）** | 计划要求的 `docs/canon_map.md` 在 8376ec2 与现 main 094861c 上都还不存在（`git cat-file -e HEAD:docs/canon_map.md` 报不在 HEAD）；它与本批 M0 收尾文档**一起提交**，尚未经 G6 独立重审，所以本行仍记开放（本节没有核它的内容）。各 G0 类门的 docstring 已按纪律 A 写明 oracle 用了哪些原语，但那不是正本核对 |
| GAP4 | tf 读取器从未被执行攻击 | verified（da5691a） | `test_tf_reader.py::test_tf_census_recomputed_through_the_readers`、`::test_tf_census_gate_has_teeth`；TF_MODE 4/5/6 改标 UNVERIFIED（`::test_tf_mode_states_4_5_6_are_unverified_not_asserted`） |
| GAP5 | 没有跨层端到端路径 | **部分**（b26d7f5、8376ec2；都是直接进 main，未经独立复核） | 三维一条已通：`tests/test_end_to_end_cb2.py`（夹具 → 盖 → 接触 → 极限分析，115 例，含 μ = √2 分界本身）。未做：bb52 二维几何 → 二维极限分析；计划里"sliding 例是平动、locked 例承载更高"也做不了——三份 bdda3d 夹具带几何、入口与节理参数（每个入口 fa = 35°；cb2 两份 coh = tens = 0，cb_bond 另有 coh = 20、tens = 3），但 cb2_locked 与 cb2_sliding 除 `case` 名外逐字段相同，step 0 导出里也没有荷载/驱动，名字里的力学判决读不出来（订正 2026-09-24：此处原写"不带摩擦/黏聚参数"，与夹具不符），所以改为对纸笔闭式 |

### M0 期间新发现、审查没抓到的缺陷（都已修）

1. **夹具溯源在新鲜克隆里是红的（cc5d7cf）。** 10 个 Phase 0 夹具入库时被 Git for Windows 的系统配置 `core.autocrlf=true`
   规范成了 LF，而 `PROVENANCE.json` 记的是工作树里 CRLF 原件的哈希；本机 git 的 stat 缓存让 `git status` 一直干净，
   所以逐字节的溯源门只在本机工作树上是绿的，从 GitHub 克隆下来它自己就是红的。六路审查全在主树里跑，没发现。
   修法：`* -text` 下 `git add --renormalize`，本仓库本地设 `core.autocrlf=false`。**由此确立一条规矩：新鲜 worktree/克隆的基线必须全绿。**
   我的复核：把本仓库克隆到一个继承系统 `autocrlf=true` 的新目录，在 b399148（修复前）`tests/test_fixture_provenance.py` 得 10 failed / 49 passed，
   在 77600c9 得 66 passed，全套 1174 passed；在 8376ec2 与 094861c 的新鲜克隆里全套都是 1209 passed。
   姊妹仓库：系统配置仍是 `autocrlf=true`（`git config --system core.autocrlf`）；b-DDA、3DDA、3-b-dda 本地都已是 `false`
   （`git -C <仓库> config --local core.autocrlf`）。3DDA 的真仓库在 `C:\3DDA\3d-DDA-work`，`C:\3DDA` 根下的 `.git` 是个空目录。
   **订正（2026-09-24）：cc5d7cf 提交说明里"3DDA 与 3-b-dda 仍继承系统的 true"，对 3DDA 是错的，不是"过时"。**
   那句话查的是 `C:\3DDA` 那个空壳 `.git`；真仓库 `C:\3DDA\3d-DDA-work` 的本地配置从 09-18 起就是 `false`
   （其 `.git/config` 修改时间 2026-09-18 16:52，早于 cc5d7cf 的 09-23 23:53）。对 3-b-dda，提交时的取值已无从回查；
   它现在是 `false`，`.git/config` 修改时间 2026-09-24 01:18、晚于 cc5d7cf，与"之后由用户设为 false"的记录相符。提交说明已推送，不改，在这里订正。
2. **LP 的三个数值缺陷（b754407，g1-lp 组在做纪律 B 的尺度格子时发现）。** (a) 表格失稳时（主元极小）仍返回 optimal，
   基解不满足 Ax = b；(b) 列按 1e2、1e4 缩放后被误判 infeasible；(c) 一阶段因舍入噪声出现"无界"（精确算术里不可能）被报成 infeasible。
   修法：按 2 的幂做行列均衡、右端项归一、加残差守卫（失稳即抛 RuntimeError）。
   看护：`test_lp_scale.py::test_numerical_breakdown_raises_instead_of_returning_a_wrong_optimum`、`::test_column_scaling_does_not_change_the_answer`、
   `::test_phase_one_rounding_noise_is_not_reported_as_infeasible`；三维黏聚块荷载 ×1e4 下承载力不超上界：
   `test_limit_duality.py::test_three_d_cohesive_block_duality_in_general_direction`。
   （修复记录里给的原始错值——残差约 1e3、承载力为解析上界的 1.46 倍——我没有在旧代码上复现，这里不引用。）
   **补（2026-09-24 复现）**：M0 前的原始链路（cc5d7cf 的 `limit.py` + `lp.py`）在三维扁块四角黏聚（μ = 0.2、各 cA = 1.5·s）、荷载 ×1e4 时**报 `optimal` 却给错值**：k = 16 时承载力是解析上界的 2.12 倍（169757 vs 夹逼 [78463, 80000]），**k = 5 时承载力为负**（−492664 vs 夹逼 [64721, 80000]）；荷载不放大（s = 1）时同一算例正确（7.919 ∈ [6.472, 8]）。复现：`git archive cc5d7cf src` 解到临时目录，跑 `tests/test_lp_fail_loud.py` 里 `_cohesive_block(1e4, 1.0)` 同款算例（2026-09-24 实跑）。
   8376ec2 另加 `tests/test_lp_fail_loud.py`（33 例，直接进 main，未经独立复核），把修复后的契约钉成门：**要么答对、要么抛
   `RuntimeError`、绝不静默给错**。格子是 lp_tol ∈ {默认, 1e-9, 1e-12, 1e-14} × 荷载/几何尺度 4 组 × k ∈ {5, 16}，共 32 格；
   在 8376ec2 上逐格跑，6 格抛错，全在显式传的 1e-12 / 1e-14 上（含黏聚 ×1e4 算例），契约允许；默认容差与 1e-9 下全部答出、落在解析夹逼内。
   默认容差下审查那个黏聚 ×1e4 算例必须答出来，不许靠抛错回避：`::test_default_tolerance_answers_the_audited_case`。
3. **三维 G0 票三在退化位形上恒红（273c984）。** 票三原来比的是凸包三角网格的全部顶点，其中含落在 facet 内部或棱上的非极点：
   四面体/方块 19 对 13、方块/方块 26 对 8（`convex_hull_3d` 的顶点数对 `_hull_extreme_vertices` 的极点数，python 命令复算）。
   以前看不见，是因为成员谓词在这些输入上先崩了，而测试只用一般位置的随机对。现在只数极点：
   `test_covers3_membership.py::test_g0_vote3_counts_only_extreme_points_on_degenerate_input`。
4. **C17 的第二轮复核判"无牙"（b1bbd27 合并 g4 组时；6f157ac 补，合并后直接进 main，未经独立复核者复核）。** 恢复的 VF/FV 带内检测只在"枚举完再追加的假盖"上测过：把 `compare()` 的枚举窗口
   从 `max(window, band)` 改回 `window`，真实的带内内核盖就进不了枚举，全套照绿。补的牙让内核顶点法锥判据恒返回"边界"，
   在 |gap| = 1.0 处产出真的假 VF 盖，门必须红。我的复核：只做这一处变异，`tests/test_g2_bdda3d.py` 得 3 failed（正是新牙的三个夹具）/ 55 passed，
   未变异 58 passed。
5. **端到端门写出来就暴露了两件事（b26d7f5，8376ec2 又改了一轮，094861c 跟改 `limit.py` 模块文档；三次都直接进 main，未经独立复核）。** 其一，对偶机构不唯一：
   推力恰落在八边形内接锥的一条棱上时，求解器返回的滑动机构切向速度偏 −22.5°（vy/vx = −tan(π/8)；在 8376ec2 上用
   `test_end_to_end_cb2._capacity(CASES[0], μ, (1,0))` 对 μ = 0.3/0.4/0.95 复算仍是如此）；承载力与对偶间隙都精确。
   所以门不读机构的原始分量。b26d7f5 版改读的"不变量"里有一条是假的：**"滑动无转动"不是对偶最优面的不变量**——μ = 0.4 时
   带转动的 (1, 0, 0.4, 0.117, 0, −0.293) 同样容许、上限同样等于 α（反例记在该测试 docstring），那条断言成立与否取决于单纯形挑了哪个顶点。
   8376ec2 删掉了 `test_failure_mode_is_read_off_the_dual_mechanism`，换成 `test_failure_mode_is_read_off_invariants_of_the_dual_face`：
   用不调用 `eab.limit.wrench` 的独立刚体虚功率核算，断言返回的机构是最优对偶；滑动时平动 + 关联剪胀 T = (1,0,μ,0,0,0) 也是最优对偶；
   倾覆时绕前缘转动 R = (0,0,1,0,1,0) 是最优对偶、且任何纯平动的上限 ≥ μW > α——所以"带转动"只在倾覆时是真不变量；μ = 1 两者并列。
   其二，cb2 的四个接触点关于原点中心对称，"接触列力矩整体反号 / 接触列忘了减参考点"这类缺陷在原点取对称中心时**看不见**。
   现加参考点不变性门 `test_capacity_is_independent_of_the_moment_reference_point`（原点 (5,−3,2)、(−0.7,0.4,−11)）。
   我的复核（只改接触列、不动荷载列；scratchpad `e2e_mutants.py`）：在 b26d7f5 上，力矩反号 → 6 例红（参考点门 4 例 + 滑动分支读机构的 2 例），
   忘减参考点 → 4 例红，全在参考点门上。在 8376ec2 上两种变异都只红参考点门那 4 例（4 failed / 111 passed）。
   **订正（2026-09-24）：b26d7f5 的提交说明与当时的测试 docstring 说"力矩反号变异下其余测试全绿"，在 b26d7f5 上是错的，而且那 2 例红也不算"读机构的门抓到了缺陷"。**
   参考点在对称中心时，力矩反号只是把四角的列互相置换，还是同一个 LP；被它弄红的那 2 例是上面那条脆断言（换了个顶点），不是抓到了缺陷。
   这个变异真正的牙只有参考点门。
   `src/eab/limit.py` 模块文档的诚实条款（第 41 行）原先仍把"是否转动"写成无条件的不变量、仍指向已删除的
   `test_failure_mode_is_read_off_the_dual_mechanism`；094861c 已改正：门改指 `test_failure_mode_is_read_off_invariants_of_the_dual_face`，
   "必带转动"只在任何纯平动的上限都严格大于 α 时（倾覆）才是不变量（`git grep read_off_the_dual_mechanism HEAD -- '*.py'` 无结果）。
   094861c 同样直接进 main、未经独立复核，待 G6。

### 仍未完成

- **G6 重审没跑。** M0 退出要求重跑六路审查、0 critical 0 major；在那之前，上表的"verified"只说明"这 31 条修对了"，不说明没有第 32 条。
- GAP1（三维首入 + 退化盖合并）→ 里程碑 M2；凹块成员判定 → 里程碑 M3；GAP3 正本对代码 → `docs/canon_map.md` 与本批文档一起提交，经 G6 复核后才能关；GAP5 的二维端到端 → 未排期。
- G2 只对账 np ↔ VF/FV，ee ↔ EE 的键没实现（带 ee 的夹具目前必红），随里程碑 M2 的入口选择一起做。
- 本表的勘误（`confirmed[5].fix` 的符号、`confirmed[23]/[27].fix` 只减 FF 不够等）已记入 `audit_20260921.json` 顶层的 `errata`；逐 id 的最终状态记入 `m0_resolution`。
