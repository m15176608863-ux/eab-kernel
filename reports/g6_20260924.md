# G6 独立重审（2026-09-24，HEAD 8200ba6）：**未通过**

M0 的退出判据是本次重审 0 critical、0 major。结果：**6 critical、7 major、18 minor，另有 3 处 major 级盲区**。
31 条发现全部被另一路用代码复现确认，0 条推翻、0 条存疑。机器可读全文：`reports/g6_20260924.json`。

十一路并行：七个审查镜头（LP/极限分析、三维几何、理论-代码一致、二维与读取器、门的牙、声称对照代码、
**五个未经复核的直接提交**）+ 四路**回归重放**（09-21 的 31 条原始缺陷逐条在新代码上重放，并在原例旁换输入再试）。
全部在新鲜 worktree `D:/eab-wt/g6`（detached 8200ba6）上读和跑。

## 一句话结论

**不是 31 个零散缺陷，是一个系统性根因：绝对容差遍布内核，而测试从不平移、从不旋转、从不放到工程坐标上。**
M0 的"纪律 B 尺度格子"只把荷载与黏聚一起放大、块体始终在原点且轴对齐——所以 6 条 critical 里有 5 条它一个都碰不到。

## Critical（6）

| # | 位置 | 失败 | 根因 |
|---|---|---|---|
| 1 | `lp.py:70` | 边长 100 m 的岩块按 SI 输入，**报 `optimal`、承载力 0**（闭式 0.6）；cb2 挪到 UTM 坐标同样返回 0 | 目标行只按列因子缩放、从不归一，入基检验用绝对 1e-10 |
| 2 | `lp.py:77` | 块体放在 (1000, 2000, 300) m、默认原点，**报 unbounded、承载力 ∞**（真值 2.57e8，不安全一侧） | 比值检验用绝对阈值，黏聚上限行在均衡后掉出 |
| 3 | `covers3.py:155` | 非轴对齐的平行面凸块对，VV3 等 E 顶点消失，**`body_distance` 返回 (inf, None)**、膨胀成员判错 | 四点单形退化判据是绝对 1e-300 |
| 4 | `covers3.py:80` | 间隙 < 1e-7（原点附近）或 < 1e-5（坐标 1e3），真最近 VE3/EV3 对被拒，**距离报 2.0000001 而非 1e-7，裕度层判"安全"** | 棱正交性判据对单位向量用绝对容差，舍入误差 ∝ 坐标/间隙 |
| 5 | `geom3.py:504` | 两块等高板 X 形交叉真实贯入，**判 −1（分离）**；随机交叉 12%–38% 漏判 | 无顶点在体内、棱交点全在共面上，证人采样漏掉细交集 |
| 6 | `margin.py:90` | 同 5 的输入，**裕度层重新 fail-unsafe**（M0 修过的 C14 回来了） | 贯入守卫依赖 5 |

## Major（7）

- `kernel2d/geom.py:207` —— `interior_point` 取离 v 最远而非离直线 pq 最远的顶点，不是 O'Rourke 规则；0.1–0.25% 的简单多边形返回体外点，**二维 G0 的真值 oracle 本身会错**（bb52 已发表数字恰好不受影响）。
- `geom3.py:607` —— `convex_hull_3d` 绝对容差；旋转后 ×1e3 的块对，凸包非流形（体积 1387 vs 真值 144）或 ZeroDivisionError，G0 假红。
- `limit.py:133` —— `contacts_from_covers` 把整张面的面积给每个顶点接触当黏聚面积；cb2 上 c=1 时承载力 67 而非 7（**16 倍高估，不安全一侧**）；EE 接触静默丢黏聚。
- `lp.py:231` —— `_duals` 绝对主元截断，大块体带黏聚时对偶行乘子被置 0，**上限少 41.8%**；远原点时 phase 2 停在对偶不可行基。README "自带上下限自校验 ✔" 失去支撑（α 本身仍对）。
- `lp.py:212` —— 默认容差下普通黏聚块体（5×3×2.5 m、c=100 kPa）**抛 RuntimeError**；另见 k=128 时 Bland 规则在浮点下**循环**（409 步周期），与 docstring"保证有限步终止"不符。契约（不静默给错）成立，但"默认容差稳健"只在枚举格子上成立。
- `tests/test_limit_duality.py:44` —— 黏聚列与 row_duals **没有有牙的门**：三个变异（黏聚力矩反号、二维黏聚单向、row_duals 反序）全套 1209 照绿。纪律 E 只用在了摩擦列上。
- `tests/test_tf_reader.py:70` —— tf 读取器的执行门不比对法向、接触点、法向力、剪切；牙测试把缺陷注入读取器**输出**（AGENTS.md 禁止的做法）；五个读取器变异全套照绿。

## 三处 major 级盲区（完整性批评者）

1. **G2 的"legacy"是手写期望清单，不是检测器输出。** `D:/3-b-dda/src/bdda3d/cases.py:110-127`（只读查看）用四个字面量 `_np_entrance(...)` 调用手工给出 cb2 的 4 个 n-p 入口，三角形 (4,7,6)/(4,6,5) 是手挑的。
   所以"三维接触枚举第一次 legacy 对账"应改为"**与 3-b-dda 手写期望清单对账**"——它验证了内核与人对该位形的理解一致，**没有**验证与任何既有检测器一致。
2. **G4 的真值口径有问题。** `tools/g4_multistep.py` 以"下一步携带进来的 m0_2（键缺失即判张开）"为真值，而本仓库自己的读取器（`bdda_df.py:98-104`）以 df22 最后一轮开闭迭代的 m0_new 为该步终判。按后者，82 个"翻转"里 21 个是转移/移除假象。
   **"砖一静态版关闭"这个否定结论需要按正确真值重算**；结论可能不变，但现有论证不成立。
3. **M0 处置表高估了堵上的盲区。** "六处堵上三处（GAP0、GAP2、GAP4）"不成立：GAP0 的 oracle 有缺陷（见 major 1），GAP2 只是形式上堵（哈希看护的是手写清单），GAP4 的门不比对物理字段（见最后一条 major），GAP5 的接缝仍会静默给错值。

## Minor（18，逐条见 JSON）

要点：`g2_bdda3d.json` 过期；`brute_ray_exit` 对非单位 u 少除 |u|²；`g0_convex3` 在尺度 ≤1e-5 时凸包报共面；`polyhedra_overlap` 默认 tol=0 时面接触判成贯入；
`merge_coplanar` 在 1e5 级坐标下共面不归并；逃逸高度的并列容差与谓词容差不一致（陡坡上 tan ψ 可报错分支）；`check_duality` 的容许性用绝对 1e-7；
右端项仅在 max b > 1 时归一；放宽 lp_tol 到 1e-4 时静默返回次优；浅于 geom_tol 的贯入跳过哨兵；bdda_df 的 C20 只修了一半（feature_b 未经 alias）；
票二 `local_only` 分支无门；`enumerated_window >= band` 是同义反复（C17 的真牙在注入测试里）；L3==L5 "零共用代码"的说法两处仍未订正（实测共用 34 个函数）；
缺"无顶点在体内"的贯入门；G2 只检查 legacy 列出的块对；G2 的带只管 VF/FV，注释说"低维盖在 gap 1.0 合法存在"是错的；canon_map 结论 1 夸大；
tia 报告"h 每减半误差降 16 倍"应为 4 倍；`bb52_g0_first_look.json` 仍记着已撤回的 1186/1186。

## 对 M0 的判定与下一步

**M0 未通过。** 按计划不降低门槛，进入 M0.2。这次不按条目修，按根因修：

1. **先写"刚体运动与单位不变性"元门（纪律 F，新增）**：每个几何/力学 API 的结果在随机旋转 + 大平移（1e3–1e5）+ 单位缩放下必须按应有的方式变换
   （距离 ×s、承载比不变、状态不变、标签一致置换）。先在当前 HEAD 上跑出红，作为这批 critical 的统一复现。
2. **统一容差政策**：几何侧以特征长度 L 与坐标量级 C 定容差（rel·L 与 eps·C 取大），替换全部绝对阈值；
   LP 侧目标行归一、比值检验与对偶求解改相对阈值、解后做对偶可行性与无界射线校验、`limit_load` 内部把力矩参考点移到接触形心（旋量精确变换，调用方不感知）。
3. `polyhedra_overlap` 补共面交叉的精确证人（棱-棱交点内推、共面同向面二维重叠内推），oracle 必须独立。
4. 其余逐条；G2 改口径为"手写期望清单"；G4 按 df22 终判重算并如实报结论。
5. 再跑 G6。
